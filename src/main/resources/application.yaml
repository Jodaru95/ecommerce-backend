spring:
  application:
    name: ecommerce-backend

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:MiClaveSuperSeguraMiClaveSuperSegura123456}
  expiration: 86400000